import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';

function Courses() {
    return (
        <div>
            <h1>Courses Page</h1>
        </div>
    );
}

export default Courses;